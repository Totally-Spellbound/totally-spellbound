---
phase: 01-foundation-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sections/spellbound-homepage.liquid
  - templates/index.json
  - temp.tsx
  - new-celestial.html
autonomous: true
requirements: [FOUND-01, FOUND-02]

must_haves:
  truths:
    - "Homepage renders without errors at /"
    - "Homepage section is configurable in Shopify theme editor (heading, subheading editable)"
    - "All other core pages still render correctly (product, collection, cart, search, page, 404)"
    - "No Liquid errors in browser console or dev server output"
  artifacts:
    - path: "sections/spellbound-homepage.liquid"
      provides: "Minimal skeleton homepage section with schema for theme editor"
      contains: "{% schema %}"
    - path: "templates/index.json"
      provides: "Homepage template wired to single spellbound-homepage section"
      contains: "spellbound-homepage"
  key_links:
    - from: "templates/index.json"
      to: "sections/spellbound-homepage.liquid"
      via: "section type reference"
      pattern: '"type": "spellbound-homepage"'
---

<objective>
Create the foundation skeleton for the homepage and clean up tech debt files.

Purpose: The existing homepage wires to 8 custom spellbound-* sections that are in "rough first pass" state. Phase 1 replaces this with a single, clean skeleton section that renders without errors and is configurable via the Shopify theme editor. This is the structural base that Phase 2 (design system) and Phase 3 (homepage experience) will build on top of. Additionally, remove abandoned prototype files from the repo root.

Output: A clean-rendering homepage with a single configurable skeleton section, and a cleaner repo without temp files.
</objective>

<execution_context>
@/Users/andrewbeniston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewbeniston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-skeleton/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton homepage section and rewire index.json</name>
  <files>sections/spellbound-homepage.liquid, templates/index.json</files>
  <read_first>
    - templates/index.json (current homepage template with 8 spellbound sections)
    - sections/spellbound-hero.liquid (reference for existing section pattern: schema structure, CSS class naming)
    - assets/spellbound-custom.css (existing custom styles — use .spellbound-label class pattern)
    - snippets/color-schemes.liquid (available CSS custom properties for colors)
  </read_first>
  <action>
    **Step 1: Create `sections/spellbound-homepage.liquid`**

    Create a minimal skeleton homepage section with the following exact content structure:

    The section must:
    - Use a `<section>` element with class `spellbound-section spellbound-section--homepage`
    - Contain a `<div class="page-width">` wrapper (Horizon's max-width container from base.css)
    - Render `{{ section.settings.heading }}` inside an `<h1>` tag
    - Render `{{ section.settings.subheading }}` inside a `<p>` tag with class `spellbound-label`
    - Include a `{% schema %}` block with:
      - `"name": "Homepage Skeleton"` (appears in theme editor)
      - `"class": "spellbound-homepage"` (for CSS targeting)
      - A `text` setting with id `heading`, label `Heading`, default `Totally Spellbound`
      - A `textarea` setting with id `subheading`, label `Subheading`, default `Crystals, curiosities & things that make you feel something`
      - A `presets` array with one entry: `{ "name": "Homepage Skeleton" }`
    - Use NO JavaScript, NO external CSS file references, NO inline `<style>` blocks
    - All content must be rendered server-side via Liquid only

    **Step 2: Replace `templates/index.json`**

    Replace the entire content of `templates/index.json` with a minimal template that references only the new skeleton section:

    ```json
    {
      "sections": {
        "main": {
          "type": "spellbound-homepage",
          "settings": {
            "heading": "Totally Spellbound",
            "subheading": "Crystals, curiosities & things that make you feel something"
          }
        }
      },
      "order": ["main"]
    }
    ```

    IMPORTANT: Do NOT delete any existing `spellbound-*.liquid` section files from `sections/`. They must remain in the directory because other templates or future phases may reference them. Only the `templates/index.json` wiring changes — the section files stay.
  </action>
  <verify>
    <automated>grep -q 'schema' sections/spellbound-homepage.liquid && grep -q 'spellbound-homepage' templates/index.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sections/spellbound-homepage.liquid exists and contains `{% schema %}`
    - sections/spellbound-homepage.liquid contains `"name": "Homepage Skeleton"`
    - sections/spellbound-homepage.liquid contains `class="page-width"`
    - sections/spellbound-homepage.liquid contains `{{ section.settings.heading }}`
    - sections/spellbound-homepage.liquid contains `{{ section.settings.subheading }}`
    - sections/spellbound-homepage.liquid contains `"presets"`
    - templates/index.json contains `"type": "spellbound-homepage"`
    - templates/index.json contains exactly 1 section in `"order"` array: `["main"]`
    - templates/index.json does NOT contain `spellbound-hero` or `spellbound-discovery-grid` or `spellbound-editorial` or `spellbound-oracle`
    - All existing sections/spellbound-*.liquid files still exist (13 files unchanged)
  </acceptance_criteria>
  <done>Homepage template wires to a single clean skeleton section. The section has a proper schema for theme editor compatibility. No existing section files are deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Remove abandoned prototype files from repo root</name>
  <files>temp.tsx, new-celestial.html</files>
  <read_first>
    - temp.tsx (confirm this is abandoned prototype code, not active)
    - new-celestial.html (confirm this is a prototype HTML file, not active)
  </read_first>
  <action>
    Delete the following files from the repository root:

    1. `temp.tsx` — abandoned React/TSX experimentation file (1000+ lines). Not part of the Shopify Liquid theme. No other file imports or references it.
    2. `new-celestial.html` — 116KB standalone HTML prototype. Not a Shopify theme file. No template or section references it.

    Use `git rm` to remove both files so they are staged for the next commit:
    ```bash
    git rm temp.tsx new-celestial.html
    ```

    These files are tech debt that clutter the repo and get included in `shopify theme push` operations, increasing push time unnecessarily.
  </action>
  <verify>
    <automated>test ! -f temp.tsx && test ! -f new-celestial.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - temp.tsx does not exist in the working directory
    - new-celestial.html does not exist in the working directory
    - No other files are deleted or modified by this task
  </acceptance_criteria>
  <done>Both abandoned prototype files are removed from the repository. The repo root contains only Shopify theme directories and standard config files.</done>
</task>

</tasks>

<verification>
- `sections/spellbound-homepage.liquid` exists with valid `{% schema %}` block
- `templates/index.json` references only `spellbound-homepage` section type
- All 13 existing `sections/spellbound-*.liquid` files still exist
- `temp.tsx` and `new-celestial.html` are removed
- `shopify theme check templates/index.json sections/spellbound-homepage.liquid` passes without errors (if Shopify CLI available)
</verification>

<success_criteria>
- Homepage renders a clean heading and subheading with no Liquid errors
- Homepage section appears in Shopify theme editor with configurable heading and subheading fields
- No existing section files are lost
- Repo root is clean of prototype files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-skeleton/01-01-SUMMARY.md`
</output>
