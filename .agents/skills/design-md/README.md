# Totally Spellbound design-md

This repository-owned skill maintains the root `DESIGN.md` from the live storefront and theme source.

It began from Google Labs' Stitch `design-md` skill, then was adapted to the project's actual source of truth. It is committed under `.agents/skills/design-md/` so every agent working in this repository receives the same workflow.
